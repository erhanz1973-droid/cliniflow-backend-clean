# Railway config (service root: cliniflow-backend-clean/).
# No explicit builder — Railway auto-detects Dockerfile in this directory.
# https://docs.railway.com/config-as-code/reference

[deploy]
startCommand = "node index.cjs"
healthcheckPath = "/health"
healthcheckTimeout = 300
